# Cookbook App

React Native Expo cookbook MVP based on `instructions.md`.

## Setup

Install dependencies:

```sh
npm install
```

Start the development server:

```sh
npm run start
```

## Current Stage

Implementation is tracked in `WORK_PACKAGES.md`.
